package main

import "fmt"

// SubsetSum — see README. Report whether some subset of nums sums to target.
// Must be recursive (include-or-exclude each element).
func SubsetSum(nums []int, target int) bool {
	// TODO: implement
	return false
}

func main() {
	// Quick eyeball runs — adjust freely while solving.
	fmt.Println(SubsetSum([]int{3, 34, 4, 12, 5, 2}, 9))
}
